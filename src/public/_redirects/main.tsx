# Netlify/Static Host Configuration
# This file ensures all routes serve the main index.html for client-side routing

# Redirect all paths to index.html (except for files that exist)
/*    /index.html   200
